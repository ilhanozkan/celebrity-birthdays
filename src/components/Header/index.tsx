import styled from "styled-components";

const Header = () => {
  return <Title>Celebrity Birthdays 🎂</Title>;
};

const Title = styled.h1`
  font-size: 4rem;
  text-align: center;
`;

export default Header;
